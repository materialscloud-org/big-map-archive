[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
check-manifest = ">=0.25"

[packages]
# version 12.0.0b3.dev7
big-map-archive = {editable = true, path="./site"}
uwsgi = ">=2.0"
uwsgitop = ">=0.11"
uwsgi-tools = ">=1.1.1"
importlib-metadata = "*"
python-dotenv = "==1.0.0"
invenio-app-rdm = {extras = ["postgresql", "opensearch2", "s3"], version = "==12.0.0b3.dev7"}

[requires]
python_version = "3.9"

[pipenv]
# Needed for invenio>v3.5.0a4 and invenio-communities
allow_prereleases = true
