"""
Settings for the project.

For the full list of settings and their values, see
https://inveniordm.docs.cern.ch/reference/configuration/.
"""

import os
from dotenv import load_dotenv

# create environment variables from invenio.env
# make sure that invenio.env appears in the virtual environment, in the same directory as invenio.cfg
basedir = os.path.abspath(os.path.dirname(__file__))
load_dotenv(os.path.join(basedir, 'invenio.env'))


# Flask
# =====
# See https://flask.palletsprojects.com/en/1.1.x/config/

# Define the value of the cache control header `max-age` returned by the server when serving
# public files. Files will be cached by the browser for the provided number of seconds.
# See flask documentation for more information:
# https://flask.palletsprojects.com/en/2.1.x/config/#SEND_FILE_MAX_AGE_DEFAULT
SEND_FILE_MAX_AGE_DEFAULT = 300


# Invenio-App
# ===========
# See https://invenio-app.readthedocs.io/en/latest/configuration.html

APP_DEFAULT_SECURE_HEADERS = {
    'content_security_policy': {
        'default-src': [
            "'self'",
            'data:', # for fonts
            "'unsafe-inline'",  # for inline scripts and styles
            "blob:",            # for pdf preview
            # Add your own policies here (e.g. analytics)
        ],
    },
    'content_security_policy_report_only': False,
    'content_security_policy_report_uri': None,
    'force_file_save': False,
    'force_https': True,
    'force_https_permanent': False,
    'frame_options': 'sameorigin',
    'frame_options_allow_from': None,
    'session_cookie_http_only': True,
    'session_cookie_secure': True,
    'strict_transport_security': True,
    'strict_transport_security_include_subdomains': True,
    'strict_transport_security_max_age': 31556926,  # One year in seconds
    'strict_transport_security_preload': False,
}


# Flask-Babel
# ===========
# See https://python-babel.github.io/flask-babel/#configuration

# Default locale (language)
BABEL_DEFAULT_LOCALE = 'en'
# Default time zone
BABEL_DEFAULT_TIMEZONE = 'Europe/Zurich'


# Flask-Mail
# ==========
MAIL_MAX_EMAILS = None
MAIL_ASCII_ATTACHMENTS = False

#: Disable email sending by default.
#: if set False will send email
#: if set True will NOT send email
MAIL_SUPPRESS_SEND = False

#: Email subject for account registration emails.
SECURITY_EMAIL_SUBJECT_REGISTER = 'Welcome to BIG-MAP Archive for BATTERY2030+'


# Invenio-I18N
# ============
# See https://invenio-i18n.readthedocs.io/en/latest/configuration.html

# Other supported languages (do not include BABEL_DEFAULT_LOCALE in list).
I18N_LANGUAGES = []


# Invenio-Theme
# =============
# See https://invenio-theme.readthedocs.io/en/latest/configuration.html

# Frontpage title
THEME_FRONTPAGE_TITLE = 'Welcome to BIG-MAP Archive for BATTERY2030+'

# Header logo
THEME_LOGO_LEFT = 'images/battery2030.png'
THEME_LOGO_RIGHT = 'images/big-map.png'


# Invenio-App-RDM
# ===============
# See https://invenio-app-rdm.readthedocs.io/en/latest/configuration.html
APP_RDM_BATTERY2030_COMMUNITY = 'battery2030'

# Instance's theme entrypoint file. Path relative to the ``assets/`` folder.
INSTANCE_THEME_FILE = './less/theme.less'


# Invenio-Files-Rest
# ==================
FILES_REST_STORAGE_FACTORY='invenio_s3.s3fs_storage_factory'


# Invenio-S3
# ==========

# Allow S3 endpoint in the CSP rules
S3_ENDPOINT_URL = os.getenv('INVENIO_S3_ENDPOINT_URL')
APP_DEFAULT_SECURE_HEADERS['content_security_policy']['default-src'].append(
    S3_ENDPOINT_URL
)


# Invenio-Records-Resources
# =========================
# See https://github.com/inveniosoftware/invenio-records-resources/blob/master/invenio_records_resources/config.py
from datetime import datetime
APP_RDM_DEPOSIT_FORM_DEFAULTS = {
    'publication_date': lambda: datetime.now().strftime("%Y-%m-%d"),
    'rights': [
        {
            'id': 'cc-by-4.0',
            'title': 'Creative Commons Attribution 4.0 International',
            'description': ('The Creative Commons Attribution license allows '
                            're-distribution and re-use of a licensed work '
                            'on the condition that the creator is '
                            'appropriately credited.'),
            'link': 'https://creativecommons.org/licenses/by/4.0/legalcode',
        }
    ],
    'publisher': 'BIG-MAP Archive',
}

# See https://github.com/inveniosoftware/invenio-app-rdm/blob/master/invenio_app_rdm/config.py
APP_RDM_DEPOSIT_FORM_AUTOCOMPLETE_NAMES = 'search' # 'search_only' or 'off'

# Invenio-RDM-Records
# ===================
# See https://inveniordm.docs.cern.ch/customize/dois/
DATACITE_ENABLED = False
DATACITE_USERNAME = ''
DATACITE_PASSWORD = ''
DATACITE_PREFIX = ''
DATACITE_TEST_MODE = True
DATACITE_DATACENTER_SYMBOL = ''

from invenio_records_resources.services.custom_fields import TextCF
from marshmallow import validate
from invenio_vocabularies.services.custom_fields import VocabularyCF

RDM_CUSTOM_FIELDS = [
    TextCF(
        name='other_repository_url',
        field_args={
            'validate': validate.URL(),
            'required': False,
            'error_messages': {
                'required': 'You must provide a URL for another repository.'
            }
        },
        multiple=False,
    ),
    VocabularyCF(
        name='work_package', # give arbitrary name to field
        vocabulary_id='workpackages', # corresponds to entry in vocabularies.yaml
        dump_options=True, # True: all possible values are visible in the dropdown UI component
        multiple=True, # True: field accepts a list of values; False: single value
        field_args={
            'required': False,
            'error_messages': {
                'required': 'You must provide a work package.'
            }
        },
    ),
]

RDM_CUSTOM_FIELDS_UI = [
    {
        'section': 'Other repository',
        'fields': [
            dict(
                field='other_repository_url',
                ui_widget='Input',
                props=dict(
                    label='URL',
                    placeholder='Add URL for your data...',
                    icon='pencil',
                    description='The URL where to get your data on other repository',
                    required=False,
                )
            ),
        ]
    },
    {
        'section': 'Work packages',
        'fields': [
            dict(
                field='work_package',
                ui_widget='Dropdown',
                props=dict(
                    label='Work packages',
                    placeholder='WP1',
                    icon='tag',
                    description='You should fill this field with one or more BIG-MAP work packages.',
                    search=False, # True: user is able to search in the current available options i.e.autocomplete dropdown
                    multiple=True, # True: field accepts multiple selected values
                    clearable=True, # True: user can deselect all the selected values
                    required=False, # True: display the * symbol to indicate that field is required
                )
            ),
        ]
    }
]

from invenio_rdm_records.config import RDM_FACETS, RDM_SEARCH
from invenio_records_resources.services.records.facets import CFTermsFacet

RDM_FACETS = {
    **RDM_FACETS,
    'workpackage': {
        'facet': CFTermsFacet(  # backend facet
            field='work_package.id',  # id is the keyword field of a vocabulary
            label='Work packages',
        ),
        'ui': {  # ui display
            'field': CFTermsFacet.field('work_package.id'),
        },
    },
}

RDM_SEARCH = {
    **RDM_SEARCH,
    'facets': RDM_SEARCH['facets'] + ['workpackage']
}


# Invenio-Accounts
# ----------------
# See https://github.com/inveniosoftware/invenio-accounts/blob/master/invenio_accounts/config.py
SECURITY_REGISTERABLE = False  # local login: allow users to register
SECURITY_CONFIRMABLE = True  # require users to confirm their e-mail address
SECURITY_LOGIN_WITHOUT_CONFIRMATION = False # require users to confirm email before being able to login

# Restricted profiles cannot be found by other users
ACCOUNTS_DEFAULT_USER_VISIBILITY = 'restricted'
ACCOUNTS_DEFAULT_EMAIL_VISIBILITY = 'restricted'

# Invenio-OAuthclient
# -------------------
# See https://github.com/inveniosoftware/invenio-oauthclient/blob/master/invenio_oauthclient/config.py

OAUTHCLIENT_REMOTE_APPS = {}  # configure external login providers

from invenio_oauthclient.views.client import auto_redirect_login
ACCOUNTS_LOGIN_VIEW_FUNCTION = auto_redirect_login  # autoredirect to external login if enabled
OAUTHCLIENT_AUTO_REDIRECT_TO_EXTERNAL_LOGIN = False  # autoredirect to external login


# Invenio-UserProfiles
# --------------------
USERPROFILES_READ_ONLY = False  # allow users to change profile info (name, email, etc...)

# OAI-PMH
# =======
# See https://github.com/inveniosoftware/invenio-oaiserver/blob/master/invenio_oaiserver/config.py
OAISERVER_ID_PREFIX = 'inveniordm.com'
"""The prefix that will be applied to the generated OAI-PMH ids."""


# Invenio-Search
# --------------
SEARCH_INDEX_PREFIX = 'inveniordm-'


# Invenio-Communities
#---------------------

# Only users with admin role are allowed to create/update/delete/rename communities
from invenio_communities.permissions import CommunityPermissionPolicy
from invenio_administration.generators import Administration
from invenio_records_permissions.generators import SystemProcess

CommunityPermissionPolicy.can_create = [Administration(), SystemProcess()]
CommunityPermissionPolicy.can_update = [Administration(), SystemProcess()]
CommunityPermissionPolicy.can_delete = [Administration(), SystemProcess()]
CommunityPermissionPolicy.can_rename = [Administration(), SystemProcess()]

# Text displayed for review policy selection
from invenio_communities.views.communities import REVIEW_POLICY_FIELDS

REVIEW_POLICY_FIELDS = [
    {
        'text': 'Review all submissions',
        'value': 'closed',
        'icon': 'lock',
        'helpText': 'All submissions to the community must be reviewed.',
    },
    {
        'text': 'Allow readers, curators, managers, and owners to publish without review',
        'value': 'open',
        'icon': 'group',
        'helpText': 'Submissions to the community by default requires review, but readers, curators, managers, and owners can publish directly without review.',
    },
]

# All community members can include a record directly, without a review
from invenio_records_permissions.generators import Disable
from invenio_communities.generators import (CommunityMembers, IfPolicyClosed)

CommunityPermissionPolicy.can_include_directly = [
    IfPolicyClosed(
        'review_policy',
        then_=[Disable()],
        else_=[CommunityMembers()],
    ),
]

# Allow readers to publish without review
from invenio_rdm_records.requests.community_submission import CommunitySubmission

CommunitySubmission.needs_context = {
        'community_roles': ['owner', 'manager', 'curator', 'reader'],
}

# Invenio-Draft-Resources
#========================

# Remove url rule for POST requests sent to /api/records/{id}/draft/actions/publish
# to avoid publications outside communities
from flask_resources import route
from invenio_drafts_resources.resources.records.resource import RecordResource

def create_url_rules(self):
    """Create the URL rules for the record resource."""
    routes = self.config.routes

    def p(route):
        """Prefix a route with the URL prefix."""
        return f"{self.config.url_prefix}{route}"

    def s(route):
        """Suffix a route with the URL prefix."""
        return f"{route}{self.config.url_prefix}"

    rules = [
        route("GET", p(routes["list"]), self.search),
        route("POST", p(routes["list"]), self.create),
        route("GET", p(routes["item"]), self.read),
        route("PUT", p(routes["item"]), self.update),
        route("DELETE", p(routes["item"]), self.delete),
        route("GET", p(routes["item-versions"]), self.search_versions),
        route("POST", p(routes["item-versions"]), self.new_version),
        route("GET", p(routes["item-latest"]), self.read_latest),
        route("GET", p(routes["item-draft"]), self.read_draft),
        route("POST", p(routes["item-draft"]), self.edit),
        route("PUT", p(routes["item-draft"]), self.update_draft),
        route("DELETE", p(routes["item-draft"]), self.delete_draft),
        #route("POST", p(routes["item-publish"]), self.publish),
        route("GET", s(routes["user-prefix"]), self.search_user_records),
    ]

    if self.service.draft_files:
        rules.append(
            route(
                "POST",
                p(routes["item-files-import"]),
                self.import_files,
                apply_decorators=False,
            )
        )

    return rules

RecordResource.create_url_rules = create_url_rules